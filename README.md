# homebrew-tap

Homebrew tap for [orca-limit-watchdog](https://github.com/johncioni/orca-limit-watchdog).

```sh
brew install johncioni/tap/orca-limit-watchdog
```

After installing, the daemon is **not** running until you start it:

```sh
orca-limit-watchdog doctor
orca-limit-watchdog start
```
